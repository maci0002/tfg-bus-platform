spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bus_platform
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
